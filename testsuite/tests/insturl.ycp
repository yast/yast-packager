{
    include "testsuite.ycp";
    map READ = $[
        "target" : $[
            "size" : 1,
            "tmpdir" : "/tmp",
        ],
        "etc" : $[
            "install_inf": $[ "Instmode":"ftp" ]
        ]
    ];

    TESTSUITE_INIT([READ, $[], $[]], nil);
    import "InstURL";
    
    TEST (``(InstURL::installInf2Url("")), [], nil);
}
