#
# ycp.exp
# 'main' file for all ycp tests
#
return 0
# get all files matching tests/*.ycp

set filenames [glob $srcdir/tests/*.ycp]

# foreach file, call tests-run (from testsuite/lib)

foreach file $filenames { tests-run $srcdir $file }
