/**
 * File:
 *   package.scr TODO
 * Summary:
 *   package agent for handle patches.
 * Access:
 *   read/write/execute OR read-only OR execute-only
 * Authors:
 *   Stefan Schubert <schubi@suse.de>
 * See:
 *   source/y2a_package/doc/package.html
 **
 * $Id$
 *
 * <p><b>Purpose:</b>
 * The package agent handles all package activities while installation <br>
 * and update.
 * </p>
 * 
 * <p><b>Implementation:</b>
 * The agent reads all package informations in the ../suse/setup/descr<br>
 * directories, gives all package informtions and solve dependencies.
 * </p>
 * 
 * <p><b>package repository paths:</b>
 * The package agent repository is accessed with the path prefix .package 
 * followed by sub-paths.</p>
 * 
 * </pre></p>
 */
.package

`ag_package ()
